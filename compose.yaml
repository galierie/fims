# For defaults
name: fims

services:
  db:
    image: postgres:17-alpine
    restart: always
    ports:
      - 5432:5432
    volumes:
      - postgres:/var/lib/postgresql/data
    networks:
      - db-network

volumes:
  postgres:

networks:
  db-network:
